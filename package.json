{
  "name": "digital-twin-avatar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Gêmeo digital 3D em tempo real a partir da câmera (pose tracking + Three.js)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "fetch-model": "node scripts/fetch-model.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
